<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.taxi.clickcar">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".Register"
            android:label="@string/label_register"
            android:logo="@drawable/logo11"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.MyRegStyle">

        </activity>
        <activity android:name=".Order"
            android:screenOrientation="portrait"
            android:label="Order"
            android:logo="@drawable/logo11"
            android:theme="@style/Theme.MyRegStyle"
            />
        <activity android:name=".ConfirmCode"
            android:screenOrientation="portrait"
            android:label="@string/confirm_code"
            android:logo="@drawable/logo11"
            android:theme="@style/Theme.MyRegStyle"
            >

        </activity>
        <activity
            android:name=".Intro.ViewPagerActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
